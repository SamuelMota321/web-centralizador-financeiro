# Base URL da API do backend (contrato OpenAPI sob /api/v1).
# Local: suba o backend com `docker compose up` no repositorio backend-centralizador-financeiro.
# Se as portas foram remapeadas (compose.override.yaml), use http://localhost:3100/api/v1.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1

# Auth0 — usado na fase 3 (S1-04). Nunca colocar credenciais reais neste arquivo.
#
# O backend valida access tokens contra o mesmo issuer e a mesma audiencia:
#   AUTH0_ISSUER_BASE_URL  (ex.: https://seu-tenant.us.auth0.com/)
#   AUTH0_AUDIENCE         (ex.: https://api.centralizador-financeiro.local)
#
# O cliente web precisa de:
# AUTH0_DOMAIN=
# AUTH0_CLIENT_ID=
# AUTH0_CLIENT_SECRET=
# AUTH0_AUDIENCE=
# APP_BASE_URL=http://localhost:3001
